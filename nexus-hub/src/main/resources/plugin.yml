name: "${project.name}"
version: "${project.version}"
website: "${project.parent.url}"
author: Agon KOLGECI

load: POSTWORLD
api-version: 1.21
main: com.agonkolgeci.nexus_hub.NexusHubBootstrap

depend: [ Nexus-API, DecentHolograms ]

commands:
  spawn:
    description: "Se téléporter au spawn."
    usage: "/<command>"
  speed:
    description: "Changer sa vitesse de marche/vol."
    usage: "/<command> <nombre>"